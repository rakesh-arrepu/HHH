# Backend environment example

# Database: use Postgres in real environments; sqlite fallback is in code for local dev
# Example for Postgres:
DATABASE_URL=postgresql://user:pass@localhost:5432/daily_tracker

# App
ENVIRONMENT=development
DEBUG=true

# Auth / Security
JWT_SECRET=replace-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXP_MINUTES=15
REFRESH_TOKEN_EXP_DAYS=7

# 2FA (TOTP)
TOTP_ISSUER=DailyTracker

# CORS
# Comma-separated if multiple
CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:5174
