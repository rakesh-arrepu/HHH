feat(graphql): mount Strawberry /graphql and add health + entries_today; align CORS; FE health check

Summary
- Enable GraphQL in backend with Strawberry and mount /graphql
- Add minimal schema: Query.health and Query.entries_today with Entry type and SectionType enum
- Align backend CORS to Vite dev origins (5173/5174); update .env.example
- Add required backend deps (strawberry-graphql, pydantic-settings)
- Implement frontend Dashboard GraphQL health check with loading/error states
- Create daily tracker and workspace change log; verify both servers are running

Backend
- backend/requirements/base.txt
  - + pydantic-settings
  - + strawberry-graphql
- backend/src/core/config.py
  - cors_allow_origins updated to ["http://localhost:5173", "http://localhost:5174"]
- backend/.env.example
  - CORS_ALLOW_ORIGINS updated to http://localhost:5173,http://localhost:5174
- backend/main.py
  - Mount Strawberry GraphQL router: GraphQLRouter(schema) at /graphql
- backend/src/api/graphql/schema.py
  - Add SectionType enum (Health, Happiness, Hela)
  - Add Entry GraphQL type (id, user_id, group_id, section_type, content, entry_date, edit_count, is_flagged, flagged_reason)
  - Add Query.health
  - Add Query.entries_today(user_id, group_id?) -> [Entry], mapping ORM to GraphQL
- backend/src/services/entry.py
  - Implement list_entries_today(db, user_id, group_id?) with filters on entry_date and deleted_at

Frontend
- frontend/src/pages/Dashboard.tsx
  - Add HEALTH_QUERY and useQuery (typed) with loading/error states
  - Display API Status card showing GraphQL health

Docs & Tracking
- DailyTasks/2025-10-21.md
  - Daily checklist and Server checks + verification outputs
- docs/WORKSPACE_CHANGES_2025-10-21.md
  - Detailed change log for today

Verification
- Backend: uvicorn backend.main:app --reload --port 8000 (running)
- GraphQL: POST /graphql { health } -> {"data":{"health":"ok"}}
- Frontend: npm install && npm run dev (Vite on http://localhost:5173) -> HTTP 200 on root
- Dashboard “API Status” reports GraphQL health ok

Notes / Follow-ups
- Next: httpOnly cookie-based auth (access/refresh) + CSRF middleware; align frontend auth flows
- Add entries/groups mutations and DataLoaders
- Add tests (backend unit/integration; frontend component)
- Add CI pipelines per .clinerules

BREAKING CHANGE: none
