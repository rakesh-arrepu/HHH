version: '3.8'

services:
  backend:
    environment:
      - ENV=development
    volumes:
      - ./backend:/app
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8000

  frontend:
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/app
    command: npm run dev
